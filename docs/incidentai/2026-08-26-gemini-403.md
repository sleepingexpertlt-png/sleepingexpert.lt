# INCIDENTAS 2026-08-26: GEMINI_API_KEY 403 → blog pipeline sustojo

Statusas: **UŽDARYTA 2026-08-26 15:38**. Aptikta ieškant Letta embedding gedimo šaknies.

## FINALAS

Šaknis: `.env` raktas buvo negyvas/atšauktas — GCP konsolėje esantys raktai veikė.
Projektas, billing, API enable, rakto apribojimai buvo tvarkingi (visi patikrinti).
Owner sukūrė/paėmė veikiantį raktą ir įrašė pats (agentai kredencialų neliečia —
frontier-agent CLAUDE.md taisyklė, pasitvirtinusi šioje sesijoje 3 kartus).

Patvirtinta gyvai: gemini-2.5-flash HTTP 200; letta-server perkurtas; 7 systemd
servisai perkrauti; `hermes_letta_add` praėjo (passage-2995d7bc, passage-dca09a63);
`hermes_letta_query` grąžina abu naujus faktus → Google spam-update vadovėlis
importuotas į Letta atmintį.

R2 (fallback) UŽDARYTA: `llm_router.py` ultimate fallback DeepSeek → Anthropic,
commit'inta, įrodyta gyvu testu per realų `_call_gemini_direct` kelią (Gemini 404
→ Anthropic per 2.9 s, matoma cost_log).

R3 (tylus gedimas: scheduler rodė `errors: []` su 0 straipsnių) — LIEKA ATVIRA,
laukia owner GO.

Rotacijos eilė (po stabilizacijos): šis Gemini raktas (nutekėjo į transkriptus),
LETTA_SERVER_PASSWORD, LETTA_PG_PASS, RankMath OAuth client secret.

## Kas atsitiko

`GEMINI_API_KEY` grąžina `403 PERMISSION_DENIED` VISIEMS modeliams — ne tik
`gemini-embedding-001` (Letta), bet ir `gemini-2.5-flash` generateContent, kuris
tą pačią dieną atliko 2 701 sėkmingą kvietimą. Paskutinis sėkmingas gemini-direct
kvietimas api_cost_log: **09:17**.

Simptomai, patvirtinti nepriklausomai (claude.ai sesija per Hermès MCP):
- `hermes_blog_today`: 2/2 paleidimai `status: error`, q=0.
- `hermes_status`: `blog_today: {error: 2}`.

## Trys atskiri radiniai

**R1 — pirminė priežastis: Google pusės leidimas.** 403 formatas (`status` laukas)
yra Google gRPC stilius, ne OpenAI. Reikia pilno klaidos kūno, kad atskirtume:
`API_KEY_SERVICE_BLOCKED` (rakto API restrictions) / `SERVICE_DISABLED` (API
neįjungtas projekte) / `CONSUMER_SUSPENDED` (projekto blokas). Laiko sąsaja:
prieš tai GCP konsolėje buvo redaguojami API rakto apribojimai.

**R2 — fallback grandinė neveikia (VPS radinys).** `llm_router.py:900` docstring'as
teigia „Ultimate fallback: Anthropic Sonnet direct", bet kodas (943–947 eil.)
naudoja `deepseek/deepseek-v4-flash`, kuris 402 Payment Required nuo 08-23.
Nėra JOKIO kelio, kuriuo nepavykęs Gemini kvietimas pasiektų Anthropic ar
OpenRouter. Gyvas testas: gemini-3.6-flash 403 → gemini-2.5-flash 403 →
groq/llama-3.3-70b „Empty response" → deepseek 402 → gemini-2.5-flash-lite 429 →
RAISED po 463.7 s.

**R3 — gedimas MASKUOTAS.** Scheduler suvestinė rodė `errors: []` ir `errors=0`,
nors 14 kategorijų bandymų davė 0 straipsnių. Stebėsena nemato tokio gedimo —
tik `hermes_blog_today` q=0 jį atskleidžia.

## Nuostoliai (VPS, patikrinta log'uose)

- Metered $: ~0 — Groq/DeepSeek/Gemini-lite atmetė prieš tokenų apmokestinimą.
- Produkcija: 2 scheduler paleidimai (09:00, 13:00 EEST), 14 kategorijų, 0 straipsnių.
  Paskutinis sėkmingas straipsnis: 2026-08-25 10:37:59 UTC.
- Paveikti: visas blog_agent pipeline (stage1_planner, stage3c_anti_slop,
  stage5_quality_gate, stage7_rankmath). NEpaveikti: niche_finder (OpenRouter),
  stage2_writer (Anthropic).
- Sąžininga išlyga (VPS): ne kiekvienas paleidimas ir sveikoje sistemoje duoda
  straipsnį (temų išsėmimas/dedup), bet 13:00 paleidimo šimtų sekundžių per
  kategoriją parašas vienareikšmiškai atitinka grandinės išsėmimą.

## Pamoka, kuri jau buvo užrašyta ir pasitvirtino

`ai-tiekeju-sprendimai-2026-08-26.md`: „netestuotas avarinis = neegzistuojantis"
(rašyta apie XAI). Čia tas pats nutiko su DeepSeek: jis buvo paskutinė gelbėjimo
grandis ir buvo miręs 3 paras — niekas to nežinojo, nes avarinis kelias niekada
nebuvo testuojamas. Fallback grandys privalo turėti periodinį liveness testą.

## Sprendimai (laukia owner GO)

1. **Šaknis:** gauti pilną 403 kūną → pagal `status` taisyti GCP (rakto API
   restrictions / API enable / projekto blokas). Jei užstrigs — naujas švarus
   raktas iš Google AI Studio.
2. **R2 minimalus fix (VPS pasiūlytas, 1 eilutė, llm_router.py:945):**
   `ultimate = "deepseek/deepseek-v4-flash"` → Anthropic modelis. Nekeičia
   FALLBACK tvarkos (nemokami lieka pirmi), tik paskutinę grandį daro gyva ir
   kitos šeimos. Docstring'as tampa teisingas.
3. **R3:** scheduler suvestinė turi rodyti `articles_written=0` kaip KLAIDĄ
   (Telegram alert), o ne `errors: []`. Atskiras GO.
4. **Ateičiai:** dokumentuota fallback grandinė (Pagrindinis → Groq →
   OpenRouter(:free) → XAI) + ketvirtinis kiekvienos grandies liveness testas.
